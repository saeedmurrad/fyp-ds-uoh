# Final / thesis notebooks

- **`dr_binary_detection_thesis_final.ipynb`** — Copy of the main binary training pipeline with a **top markdown cell** summarising thesis results (Ch. 6) and the path to the PDF report. Use this for **defence / hand-in** if you want the extra context in one file.

- **Source of truth for code** — The repository keeps the same cells in `../dr-detection-training-final_updated.ipynb`. Both notebooks write metrics and figures to **`results/`** (see project `results/README.md`).
